void kernel_main(void) {
    char* vidmem = (char*)0xB8000;
    const char* msg = "Kernelul ruleaza!";
    int i = 0;

    while (msg[i]) {
        vidmem[i * 2] = msg[i];
        vidmem[i * 2 + 1] = 0x0F; // alb pe negru
        i++;
    }

    for (;;) {} // loop infinit
}
